const initialState = {
    currentUser: {},
    isAuth: false,
}

export default userReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}